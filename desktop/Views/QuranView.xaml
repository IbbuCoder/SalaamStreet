<UserControl x:Class="SalaamStreet.Desktop.Views.QuranView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Surah list -->
        <Border Grid.Column="0" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBox x:Name="Search" DockPanel.Dock="Top" Margin="10" Padding="8"
                         Background="{StaticResource Surface2}" Foreground="{StaticResource Text}"
                         BorderBrush="{StaticResource Border}" TextChanged="Search_Changed" />
                <ListBox x:Name="SurahList" BorderThickness="0" Background="Transparent"
                         Foreground="{StaticResource Text}" SelectionChanged="Surah_Changed"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
            </DockPanel>
        </Border>

        <!-- Reader -->
        <DockPanel Grid.Column="1">
            <Border DockPanel.Dock="Top" Padding="20,14" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Style="{StaticResource GhostButton}" Content="A-" Click="FontSmaller_Click" Margin="0,0,6,0" />
                        <Button Style="{StaticResource GhostButton}" Content="A+" Click="FontLarger_Click" Margin="0,0,6,0" />
                        <Button Style="{StaticResource GhostButton}" Content="⤢ Study (F11)" Click="Study_Click" Margin="0,0,6,0" />
                        <Button Style="{StaticResource PrimaryButton}" x:Name="DownloadBtn" Content="Download offline" Click="Download_Click" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock x:Name="SurahTitle" Text="—" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Text}" />
                        <TextBlock x:Name="SurahMeta" Text="" Style="{StaticResource Muted}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <ScrollViewer x:Name="ReaderScroll" VerticalScrollBarVisibility="Auto" Padding="28">
                <ItemsControl x:Name="AyahList" MaxWidth="900" HorizontalAlignment="Center" />
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
